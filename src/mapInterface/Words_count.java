package mapInterface;

public class Words_count {
    public static void main(String[] args){
        String s="java is a language";
        String[] s2=s.split("\\s");
        System.out.println(s2.length);//we stored in array so use array length
    }
}
